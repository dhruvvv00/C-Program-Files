#include<stdio.h>
#include<math.h>
//here the output will be 1 then it means then the statement is true 
//if it shows 0 then it means that the statement is flase
int main(){
//== means we are comparing if the  values are equal or not
printf("%d \n",4==4 );
return 0;
}